scalar DateTime

# Types may not be empty: https://github.com/apollographql/graphql-tools/issues/648
# They're extended in the modules.
type Query {
  _empty: String
}
type Mutation {
  _empty: String
}
type Subscription {
  _empty: String
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
