const path = require('path')
require('./build/config/env')
const config = require('./build/config/database')

module.exports = {
  'url': `${config.dialect}://${config.username}:${config.password}@${config.host}/${config.database}`,
  'migrations-path': path.resolve(__dirname, 'build', 'migrations'),
}
